# Xsens MVN UDP/TCP Data Parsing Python Libraries

The code was written in c++, wrapped with SWIG to python libraries. The library could be used to parse the Xsens MVN network streaming UDP/TCP data.
